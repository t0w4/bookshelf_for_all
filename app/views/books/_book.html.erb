            <div class="col-sm-4 col-md-3">
              <div class="thumbnail">
                <%= link_to image_tag(book.image, { :border => '0', :alt => '画像がありません'}), book_path(book.id) %>
                <div class="caption">
                  <%= link_to book.title, book_path(book.id) ,class: "book-info"%>
                  <p class="book-info"><%= book.publicationDate.strftime("%Y年 %m月 %d日") %></p>
                  <p class="book-info"><%= book.author %></p>
                </div>
              </div>
            </div>